# Cursor pack (this repo)

| Path | Role |
| --- | --- |
| [`plans/cursor-skills.plan.md`](plans/cursor-skills.plan.md) | **Canonical plan** — single source of truth for remaining work |
| [`scripts/`](scripts/) | **`install.sh`** — copy skills to `~/.cursor/skills` |
| [`tests/`](tests/) | Pack validation checks (`run.sh`, fixtures, registries) |
| `.gitignore` | Ignores ephemeral workspace files (e.g. `gh/` drafts) |

From repo root: **`./.cursor/scripts/install.sh`** · **`./.cursor/tests/run.sh`**
