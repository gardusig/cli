diff --git a/.github/workflows/lib/pull-request-router.yml b/.github/workflows/lib/pull-request-router.yml
index f433858..5fedd17 100644
--- a/.github/workflows/lib/pull-request-router.yml
+++ b/.github/workflows/lib/pull-request-router.yml
@@ -188,7 +188,8 @@ jobs:
             --job-json '${{ toJson(matrix.job) }}' \
             --pipeline-src pipeline-src \
             --app-src app-src \
-            --tag-prefix pipeline
+            --tag-prefix pipeline \
+            --release-version "$(python3 -c "import tomllib; print(tomllib.load(open('app-src/pyproject.toml','rb'))['project']['version'])")"
       - name: Upload Pages artifact
         if: needs.resolve.outputs.pages_enabled == 'true' && matrix.job.id == 'latex'
         uses: actions/upload-pages-artifact@v4
@@ -233,7 +234,8 @@ jobs:
             --job-json '${{ toJson(matrix.job) }}' \
             --pipeline-src pipeline-src \
             --app-src app-src \
-            --tag-prefix pipeline
+            --tag-prefix pipeline \
+            --release-version "$(python3 -c "import tomllib; print(tomllib.load(open('app-src/pyproject.toml','rb'))['project']['version'])")"
       - name: Upload Pages artifact
         if: needs.resolve.outputs.pages_enabled == 'true' && matrix.job.id == 'latex'
         uses: actions/upload-pages-artifact@v4
@@ -278,7 +280,8 @@ jobs:
             --job-json '${{ toJson(matrix.job) }}' \
             --pipeline-src pipeline-src \
             --app-src app-src \
-            --tag-prefix pipeline
+            --tag-prefix pipeline \
+            --release-version "$(python3 -c "import tomllib; print(tomllib.load(open('app-src/pyproject.toml','rb'))['project']['version'])")"
       - name: Upload Pages artifact
         if: needs.resolve.outputs.pages_enabled == 'true' && matrix.job.id == 'latex'
         uses: actions/upload-pages-artifact@v4
@@ -323,7 +326,8 @@ jobs:
             --job-json '${{ toJson(matrix.job) }}' \
             --pipeline-src pipeline-src \
             --app-src app-src \
-            --tag-prefix pipeline
+            --tag-prefix pipeline \
+            --release-version "$(python3 -c "import tomllib; print(tomllib.load(open('app-src/pyproject.toml','rb'))['project']['version'])")"
       - name: Upload Pages artifact
         if: needs.resolve.outputs.pages_enabled == 'true' && matrix.job.id == 'latex'
         uses: actions/upload-pages-artifact@v4
@@ -368,7 +372,8 @@ jobs:
             --job-json '${{ toJson(matrix.job) }}' \
             --pipeline-src pipeline-src \
             --app-src app-src \
-            --tag-prefix pipeline
+            --tag-prefix pipeline \
+            --release-version "$(python3 -c "import tomllib; print(tomllib.load(open('app-src/pyproject.toml','rb'))['project']['version'])")"
       - name: Upload Pages artifact
         if: needs.resolve.outputs.pages_enabled == 'true' && matrix.job.id == 'latex'
         uses: actions/upload-pages-artifact@v4
@@ -413,7 +418,8 @@ jobs:
             --job-json '${{ toJson(matrix.job) }}' \
             --pipeline-src pipeline-src \
             --app-src app-src \
-            --tag-prefix pipeline
+            --tag-prefix pipeline \
+            --release-version "$(python3 -c "import tomllib; print(tomllib.load(open('app-src/pyproject.toml','rb'))['project']['version'])")"
 
   pr-stage-6:
     name: PR ${{ matrix.job.name }}
@@ -453,7 +459,8 @@ jobs:
             --job-json '${{ toJson(matrix.job) }}' \
             --pipeline-src pipeline-src \
             --app-src app-src \
-            --tag-prefix pipeline
+            --tag-prefix pipeline \
+            --release-version "$(python3 -c "import tomllib; print(tomllib.load(open('app-src/pyproject.toml','rb'))['project']['version'])")"
 
   pr-stage-7:
     name: PR ${{ matrix.job.name }}
@@ -493,7 +500,8 @@ jobs:
             --job-json '${{ toJson(matrix.job) }}' \
             --pipeline-src pipeline-src \
             --app-src app-src \
-            --tag-prefix pipeline
+            --tag-prefix pipeline \
+            --release-version "$(python3 -c "import tomllib; print(tomllib.load(open('app-src/pyproject.toml','rb'))['project']['version'])")"
 
   deploy-pages:
     name: Deploy GitHub Pages
diff --git a/.github/workflows/pull-request/python-cli.yaml b/.github/workflows/pull-request/python-cli.yaml
index b115f36..c773a0e 100644
--- a/.github/workflows/pull-request/python-cli.yaml
+++ b/.github/workflows/pull-request/python-cli.yaml
@@ -1,5 +1,7 @@
 repo: gardusig/cli
 selective: true
+dockerfile: .github/Dockerfile
+dockerignore: .dockerignore
 jobs:
 - id: lint
   name: Repo lint
@@ -51,12 +53,14 @@ jobs:
     - gardusig_cli.egg-info/
     - scripts/test/
     forbid_direct_cli_references: false
+    allowed_paths:
+    - .github/pull-request.yaml
+    - .github/workflows/pull-request.yml
+    - .github/workflows/release.yml
 - id: version-check
   name: Version gate
   target: version-check
   needs: repo-hygiene
-  build_args:
-    BASE_VERSION: "1.0.3"
 - id: core-gates
   name: Core integration gates
   target: core-gates
@@ -74,7 +78,6 @@ jobs:
 - id: unit
   name: Unit tests (full suite)
   target: unit-test
-  when_full_suite: true
   needs: version-check
 - id: integration
   name: Integration tests (full suite)
@@ -86,13 +89,12 @@ jobs:
   name: TestPyPI publish (Docker)
   target: pypi-test
   needs: unit
-  build_args:
-    CLI_RELEASE_VERSION: 1.0.3
+  build_args_from_ref: true
   secrets:
     TESTPYPI_API_TOKEN: TESTPYPI_API_TOKEN
 - id: testpypi-consumer
   name: TestPyPI consumer install
   target: testpypi-consumer
   needs: pypi
-  build_args:
-    CLI_RELEASE_VERSION: 1.0.3
+  build_args_from_ref: true
+  docker_socket: true
diff --git a/.github/workflows/release/python-cli.yaml b/.github/workflows/release/python-cli.yaml
index 8b54d11..0f8d7c1 100644
--- a/.github/workflows/release/python-cli.yaml
+++ b/.github/workflows/release/python-cli.yaml
@@ -1,4 +1,6 @@
 repo: gardusig/cli
+dockerfile: .github/Dockerfile
+dockerignore: .dockerignore
 jobs:
   - id: release
     name: Publish to PyPI
@@ -6,3 +8,9 @@ jobs:
     build_args_from_ref: true
     secrets:
       PYPI_API_TOKEN: PYPI_API_TOKEN
+  - id: pypi-consumer
+    name: PyPI consumer integration
+    target: pypi-consumer
+    needs: release
+    build_args_from_ref: true
+    docker_socket: true
