# cpp

`gardusig/cpp` (public) — C++17 competitive-programming reference snippets.

```bash
cli pipeline run pull-request cpp --pipeline compile --ref my-branch
```

Interview competition solutions stay in private `gardusig/interviewing`.
