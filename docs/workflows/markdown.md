# Markdown validation

Markdown lint runs via `cli lint` when `.md` files are present in a repository.

## Checks

- `markdownlint-cli2` on `.md` / `.mdx`
- Optional Mermaid diagram render smoke tests

## In this repo

```bash
cli lint markdown docs/
```

See [toolkit.md](../toolkit.md).
