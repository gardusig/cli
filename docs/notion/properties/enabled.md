# 📌 enabled

| | |
| --- | --- |
| **Type** | select / boolean in yaml |
| **Source** | [header/](../header/) **`enabled`** field |

When **`enabled: false`**, the task is kept in git but **skipped on deploy** to Notion — use for current-life pauses (e.g. instruments, video games).

**Upstream:** [Properties](./README.md)
