# Dev + test dependencies (keep in sync with pyproject.toml [project.optional-dependencies.dev]).
# Recommended: SHUTTLE_BOOTSTRAP_DEV=1 ./scripts/bootstrap.sh  or  pip install -e ".[dev]"
-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
