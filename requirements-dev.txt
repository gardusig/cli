# Dev + test dependencies (keep in sync with pyproject.toml [project.optional-dependencies.dev]).
# Recommended: CLI_BOOTSTRAP_DEV=1 ./scripts/bootstrap.sh  or  pip install -e ".[dev]"
-r requirements.txt
build>=1.0
pytest>=8.0
pytest-cov>=5.0
pytest-timeout>=2.3
twine>=5.0
