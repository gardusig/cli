# Runtime dependencies (keep in sync with pyproject.toml [project.dependencies]).
# Recommended: ./scripts/bootstrap.sh  or  pip install -e .
typer>=0.12
pydantic>=2.0
pydantic-settings>=2.0
PyYAML>=6.0
rich>=13.0
httpx>=0.27
