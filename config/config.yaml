# Default config — override paths in .env or ~/.config/cli/config.yaml (see config.example.yaml).
backup:
  tags_dir: .integration-scratch/tags
  repositories: []

notion:
  database_id: ""
  task_root: ""
  pairs_file: config/notion/tasks.pairs.json
  link_branch: main
  link_repo: ""
  labels_manifest: labels.manifest.yaml
  cleanup_before_deploy: true

chrome:
  profile: Default
  bookmarks_file: ""
  downloads_dir: ~/Downloads
  photos_dir: ""
  photos_takeout_dir: ""
