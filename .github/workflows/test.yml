name: test

# Status check names (branch protection): "Unit tests (Docker)", "Integration tests (Docker)"
# Runs on pull requests only — merge is gated here; tags trigger release.yml.

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_dispatch:

concurrency:
  group: test-${{ github.workflow }}-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  unit:
    name: Unit tests (Docker)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build integration test image
        uses: docker/build-push-action@v6
        with:
          context: .
          target: integration
          load: true
          tags: |
            cli:integration
            cli:dev
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Unit tests in container
        env:
          CLI_DOCKER_IMAGE: cli:integration
          CLI_DOCKER_TARGET: integration
          CLI_DOCKER_SKIP_BUILD: "1"
        run: ./scripts/test-unit.sh

  integration:
    name: Integration tests (Docker)
    needs: unit
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build integration test image
        uses: docker/build-push-action@v6
        with:
          context: .
          target: integration
          load: true
          tags: |
            cli:integration
            cli:dev
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Integration tests in container
        env:
          CLI_DOCKER_IMAGE: cli:integration
          CLI_DOCKER_TARGET: integration
          CLI_DOCKER_SKIP_BUILD: "1"
        run: ./scripts/test-integration.sh
