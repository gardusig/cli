# PyPI-enabled release caller — gardusig/cli only
name: release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  pipeline:
    uses: gardusig/pipelines/.github/workflows/reusable-ci-release.yml@main
    with:
      publish_pypi: true
    secrets: inherit
