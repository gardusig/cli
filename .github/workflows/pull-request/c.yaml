repo: gardusig/c
jobs:
  - id: lint
    name: Repo lint
    target: lint
  - id: repo-hygiene
    name: Repo structure
    target: repo-hygiene
    needs: lint
    hygiene_policy:
      max_depth: 4
      allowed_root_dirs:
        - .github
        - lib
        - examples
      allowed_root_files:
        - .gitignore
        - CMakeLists.txt
        - LICENSE
        - Makefile
        - README.md
      allowed_extensions:
        - .c
        - .h
        - .in
        - .md
        - .out
        - .txt
        - .yml
      allowed_filenames:
        - .gitignore
        - CMakeLists.txt
        - LICENSE
        - Makefile
